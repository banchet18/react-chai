1. index.html is main file where all content is loaded.
2. noscript tag -> if suppose in browser javascript is not available then noscript tag shows What you write eg.you need to enable javascript to run this app.

3. react-dom is important virtual dom it is used for web browsers

4. jsx -> <App/> , <chetan/> => custom tag

5. react-script is used to connect index.html to index.js which is located in package.json file.

6. vite do not use react-script its used script tag.

7. to create component uppercase is must -> good practice.

8. by the component we can only return a single element. good practice is wrap all elements in single div or fragment.

----------- video 4 ------------

04customReact file

<App/> is function

injecting javascript in jsx

babel is important

-------- video 5 --------------

05counter file

react hook

we can give any default value in useState hook except function. eg 0,false,null,{} etc.

two arrays in useState 1st is variable 2nd is function by which variable value can change in ui wherever it written.

#assignment - in remove value it should not show lesser value than 0
0 should be end point for remove value
again in add value it should not go beyond 20 value
20 should be end point for add value

assignment complete 05counter file ...

----------- video 6 ---------------------------------

react fiber:-
https://github.com/acdlite/react-fiber-architecture

--------------------------------- video 7 ------------------------------
06tailwindprops

install tailwind
taking tailwind component
from devUi

props
arr and obj props
destructuring

---------------------------- video 8 ---------------------------
#interview question

setCounter((prepCount) => prepCount + 1);
setCounter((prepCount) => prepCount + 1);
setCounter((prepCount) => prepCount + 1);
setCounter((prepCount) => prepCount + 1);

15 value to direct 19 value through call back function

------------------------------ video 9 -------------------------------
07bgChanger

onClick wants function not function return so,
onClick(()=>{}) good practice

------------------- video 10 --------------

08passwordgenertor important !!!

----------------------- video 11 -----------------------

1. custom hook
2. fetch api
3. component
